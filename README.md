# Signal-Processing-Project
A little project where I will be using Python to generate radar-like signals, detect simulated targets, estimate their range, use Doppler to estimate their velocity, and determine whether or not (and where) a target is present. I will also add noise and analyse performance under different noise levels.
